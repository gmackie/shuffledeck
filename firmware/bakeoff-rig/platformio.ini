; Feeder singulation bakeoff rig
; ESP32 DevKit — Arduino framework
; Serial at 115200 for CSV capture

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
build_flags =
    -DCORE_DEBUG_LEVEL=0
